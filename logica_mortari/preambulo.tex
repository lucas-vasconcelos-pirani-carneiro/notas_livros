% preambulo.tex
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[brazil]{babel}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{mathtools}
\usepackage{fancyhdr}
\usepackage{geometry}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{titlesec}
\usepackage{tocloft}
\usepackage{bm}
\usepackage[many]{tcolorbox}
\renewcommand{\contentsname}{Sumário}

\geometry{a4paper, margin=2cm}
\setlength{\headheight}{16pt} % Ajuste para warning do cabeçalho

% Cores
\definecolor{logicRed}{RGB}{178,34,34}
\definecolor{logicGray}{RGB}{80,80,80}

% Títulos
\titleformat{\section}
  {\normalfont\Large\bfseries\color{logicRed}}
  {\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\large\bfseries\color{logicRed}}
  {\thesubsection}{1em}{}

% Cabeçalho e rodapé
\pagestyle{fancy}
\fancyhf{}
\rhead{\textcolor{logicRed}{Lógica}}
\lhead{\textcolor{logicGray}{Mortari}}
\cfoot{\thepage}

% Definições das cores (adicione antes de usar as boxes)
\definecolor{logicRed1}{RGB}{217, 80, 80}     % Vermelho principal
\definecolor{logicRed2}{RGB}{233, 150, 122}   % Vermelho claro/quase salmão
\definecolor{logicRed3}{RGB}{220, 67, 67}     % Vermelho mais escuro
\definecolor{logicRed4}{RGB}{250, 200, 200}   % Vermelho bem clarinho
\definecolor{logicRed5}{RGB}{170, 44, 44}     % Vermelho bem escuro

% Boxes em tons de vermelho

\newtcolorbox{defbox}{
  colback=logicRed4,
  colframe=logicRed3,
  sharp corners,
  boxrule=1pt,
  before skip=10pt,
  after skip=10pt,
  fonttitle=\bfseries\color{white},
  title=Definição
}

\newtcolorbox{theorembox}{
  colback=logicRed4,
  colframe=logicRed3,
  sharp corners,
  boxrule=1pt,
  before skip=10pt,
  after skip=10pt,
  fonttitle=\bfseries\color{white},
  title=Teorema
}

\newtcolorbox{proofbox}{
  colback=logicRed4,
  colframe=logicRed3,
  sharp corners,
  boxrule=0.8pt,
  before skip=10pt,
  after skip=10pt,
  fonttitle=\bfseries\color{white},
  title=Demonstração
}

\newtcolorbox{examplebox}{
  colback=logicRed4,
  colframe=logicRed3,
  sharp corners,
  boxrule=1pt,
  before skip=8pt,
  after skip=8pt,
  fonttitle=\bfseries\color{white},
  title=Exemplo
}
\newtcolorbox{ttbox}{
  colback=logicRed4,
  colframe=logicRed3,
  sharp corners,
  boxrule=0.8pt,
  left=4pt,
  right=4pt,
  before skip=10pt,
  after skip=10pt,
  title=Tabela-Verdade,
  fonttitle=\bfseries\color{white}
}
